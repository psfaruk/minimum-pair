# pyquotex (vendored in pyquotex/) dependencies
certifi>=2025.1.31
httpx>=0.27.0
websockets>=12.0
pyfiglet>=1.0.2
beautifulsoup4>=4.12.3
fake-useragent>=2.2.0
rich>=13.7.0
python-socks>=2.4.0

# Cloudflare challenge solver / browser-hosted websocket transport:
# playwright is intentionally NOT here — it is installed opportunistically
# by nixpacks.toml (pip install playwright || WARN). The app lazy-imports
# it, so a build without it still boots and /api/status reports the
# solver as unavailable; the 3-tier transport degrades to the python
# dial + QUOTEX_PROXY and token → live data still connects.

# app backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
